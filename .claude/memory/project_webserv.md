---
name: project-webserv
description: "Webserv is a group C++ project starting week of 2026-05-18, replacing the solo cpp_XX module pattern"
metadata: 
  node_type: memory
  type: project
  originSessionId: 272d31d1-4f6e-4f11-97b8-37a28fdd7c3e
---

Webserv starts week of 2026-05-18. It is a **team project with a shared GitHub repo** and a different structure — not a cpp_XX solo module.

**Why:** 42 curriculum progression; Webserv is a larger multi-week group project, so the solo `codam-session.sh` and `module-progress.sh` patterns don't directly apply.

**How to apply:** When building school workflow scripts, design them to work for both the cpp_XX pattern (past modules) AND a separate team project repo. The Codam session script needs to handle the team-repo case. Module progress tracking needs to know Webserv is a project, not a module.
